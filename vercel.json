{
  "buildCommand": "cd widget-landing && npm run build",
  "outputDirectory": "widget-landing/dist",
  "installCommand": "npm install && cd widget-landing && npm install",
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
