{
  "buildCommand": "npm install && cd widget-landing && npm run build && rm -rf ../dist && cp -r dist ../dist",
  "outputDirectory": "dist",
  "installCommand": "npm install && cd widget-landing && npm install"
}
